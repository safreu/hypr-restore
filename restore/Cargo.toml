[package]
name = "restore"
version = "0.1.0"
edition = "2024"

[dependencies]
listener = { path = "../listener" }
shared = { path = "../shared" }
utils = { path = "../utils" }
snapshot = { path = "../snapshot" }